spring:
  servlet:
    multipart:
      max-file-size: 10MB  #上传文件的最大大小
      max-request-size: 10MB  #请求文件的最大大小
  application:
    name: file
server:
  port: 18082
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true